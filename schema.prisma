datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Product {
    id Int @id @default(autoincrement())
    producer String @default("Mitin Fishing")
    category String
}
